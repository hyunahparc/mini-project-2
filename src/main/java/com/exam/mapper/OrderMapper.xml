<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.exam.mapper.OrderMapper">
  
  	
  
   <!-- 주문 생성 -->
    <insert id="createOrder" parameterType="com.exam.dto.OrderDTO">
        INSERT INTO orders (orderid, userid, orderDate, username, post, addr1, addr2, phone1, phone2, phone3, point)
        VALUES (#{orderid}, #{userid}, #{orderDate}, #{username}, #{post}, #{addr1}, #{addr2}, #{phone1}, #{phone2}, #{phone3}, #{point})
    </insert>

    <!-- 주문 조회 -->
    <select id="findOrderByUserId" parameterType="String" resultType="com.exam.dto.OrderDTO">
        SELECT orderid, userid, orderDate, username, post, addr1, addr2, phone1, phone2, phone3, point
        FROM orders
        WHERE userid = #{userid}
    </select>

  
	
</mapper>